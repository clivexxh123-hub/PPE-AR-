# 非 AI 功能（工程侧）— 推荐做法与计划表

> 覆盖《需求拆解_功能模块.md》M1 ~ M13；AI 侧见《AI算法_推荐做法与计划表.md》
> 人力：前端 1.5 + 小程序 0.5 + 后端 1 + PM/测试 0.5，共 10 周

---

## 一、技术选型推荐

| 层 | 推荐 | 理由 |
|---|---|---|
| 前端框架 | Vue3 + Vite + Pinia + Element Plus | 合同约定栈；Element Plus 覆盖后台/表单/表格，省工时 |
| **画布编辑器** | **Fabric.js**（备选 Konva） | 选中/拖拽/缩放/旋转/图层/序列化开箱即用，是 R5.1 全部动词；画布状态存 JSON 即模板（R5.2） |
| 小程序 | **uni-app（Vue3 版）** | 与 PC 端共用组件逻辑与 API 层；简化画布用 uni canvas 或"服务端渲图 + 参数面板"降级方案 |
| 后端 | Node.js + Express + Sequelize/Prisma | 合同约定栈；ORM 统一迁移与建模 |
| 数据库/缓存 | MySQL 8 + Redis | Redis 兼做队列（BullMQ）、分享链接缓存、防刷计数 |
| 图片处理 | sharp（缩略图/压缩/合图/水印） | 高性能，覆盖 R1.3 压缩、R6.3 水印、R10.1 合图 |
| 存储 | OSS，前端 **STS 临时凭证直传** | 大图不过 Node，省带宽；服务端只收回调登记 |
| PDF 报价单 | **Puppeteer 渲 HTML 模板** | 报价单排版用 HTML/CSS 做，美观可控，改版不改代码逻辑 |
| 部署 | Linux + Nginx + PM2 cluster + HTTPS | 合同约定；PM2 多进程扛 Web 并发 |

## 二、关键设计推荐

### 数据模型（核心表）
`products`（含 printable JSON、prompt 配置、国标/卖点富文本）、`categories`（父子多级，产品多对多）、`product_views`（视角底图 + **印刷区标定网格 JSON**）、`logos`（原图/透明图/主色，归属客户）、`customers`、`schemes`（方案，画布 JSON + 版本号）、`scheme_versions`（版本回溯，R7.3）、`templates`（行业模板）、`gen_tasks`（生成记录 + 耗时，供看板与压测）、`share_links`、`audit_logs`。

### 逐模块要点

- **M1 产品管理**：批量导入用 Excel 模板 + 后端校验报告（第几行错什么）；视角完整性按品类配置表校验（安全帽4/防撞帽2/身防2），别硬编码。**标定工具做进底图上传页**（供 AI-3，见 AI 文档）。
- **M2/M3 LOGO 与文字**：LOGO+文字组合、纯文字印字都在 Fabric 画布内做，"另存为素材"= 画布局部导出透明 PNG 入库，一套机制三处复用（R2.3/R3.1/R3.2）。中文字体版权注意：微软雅黑商用需授权，建议默认改**思源黑体**并向甲方确认。
- **M4 印刷规范**：三档尺寸做成产品级配置（甲方 W3 提供数值，先用占位值开发）；mm↔px 换算系数存在标定数据里，滑块与数值输入双向绑定。
- **M5 画布**：撤销/重做用命令栈（Fabric 状态快照）；画布 JSON 即模板与方案的存储格式，版本管理直接存快照 diff 或全量 JSON。
- **M7 客户档案**：所有资产表带 `customer_id`，归档=改归属；**数据隔离在中间件层统一做**（每个查询强制注入 customer/租户过滤），并写 2–3 个越权访问测试用例应对验收。
- **M9 KA 展示**：清单文案来自产品库字段，渲染到图片留白用 sharp 合成固定版式模板。
- **M10 分享导出**：分享链接 = 随机 token + 过期时间 + bcrypt 密码 + Redis 计数防刷；微信内嵌页出缩略图需配 og meta；多分辨率导出后端 sharp 缩放出 3 档。
- **M11 看板**：打开次数/设备来自 share_links 访问日志表；热门产品与工作量统计直接 SQL 聚合 + ECharts，数据量级不需要引入额外分析组件。
- **M12 小程序**：只做 AI 生成 + 简化画布 + 下载，共用 API；微信下载图片需配置合法域名，提前报备。
- **M13 非功能**：mysqldump 每日全备 + binlog 增量，OSS 开版本控制；操作日志中间件统一埋点；AI 异常映射为用户话术（"人太多啦，排队中"而非报错码）。

## 三、工程侧计划表（10 周）

| 周 | 后端 | 前端（PC/后台） | 小程序 |
|---|---|---|---|
| **W1** | 仓库/CI/环境；全量表设计；OpenAPI 契约；OSS+STS 打通 | 脚手架 + Element Plus 框架；**Fabric.js 画布 PoC**（拖拽/图层/序列化） | — |
| **W2** | 产品/分类 CRUD、批量导入、视角上传校验（M1） | 后台框架 + 产品管理页；**标定工具 v1**；画布骨架（R5.1） | — |
| **W3** | LOGO 库 + 客户归档 API（R2.1）；对接抠图 API | LOGO 上传/预览/换色调色板（R2.2）；文字印刷（R3.1） | — |
| **W4** | 模板/方案存取 API（R5.2）；尺寸配置 API（R4.3） | LOGO+文字组合（R2.3）；多内容叠加与图层（R3.2）；印刷面独立配置（R4.1/4.2） | — |
| **W5** | 导出管线 v1（PNG + 水印）；⛳ MS2 联调 | 画布全功能收口；核心链路端到端 | — |
| **W6** | **BullMQ 队列 + gen_tasks 落库**（对接 AI-6）；客户档案 CRUD + 自动归档（M7） | 生成流程 UI + 等待动效（R6.1）；客户档案页 | — |
| **W7** | 分享链接（R10.2）；PDF 报价单（R10.3）；方案版本 API（R7.3） | 行业模板加载（R8.1，甲方内容到位）；KA 展示（R9.1）；版本回溯 UI | 启动：登录 + AI 生成 + 下载 |
| **W8** | 看板聚合 API（M11）；多分辨率/合图导出（R10.1）；**Web 层压测配合** | Dashboard + 数据看板；⛳ MS3 功能冻结 | 简化画布 + 微信分享适配 |
| **W9** | 越权用例、备份恢复演练、审计日志补齐；Bug 修复 | 全量回归 + UAT 问题修复 | 回归 + 域名报备确认 |
| **W10** | 生产部署（Nginx/PM2/HTTPS）、数据初始化、监控告警 | 终验支持、操作手册 | 提审发布 |

**PM/测试贯穿**：W1 建验收对照表 → 每周核对 → W9 组织 UAT（销售/市场 5–10 人）→ W10 初验/整改/终验。

## 四、风险与对策

| 风险 | 对策 |
|---|---|
| 画布编辑器工作量被低估（前端最大坑） | W1 就做 Fabric PoC 验证全部交互动词；小程序画布提前定"简化版"边界并让甲方签认 |
| 甲方素材逾期（尺寸/模板/文案/水印） | 占位数据先行，素材到位仅替换；逾期书面留痕顺延 |
| 微软雅黑字体版权 | W3 前与甲方确认商用授权或改思源黑体 |
| 小程序审核周期不可控 | W8 完成开发即首次提审，预留一轮驳回缓冲 |
| 服务器/域名/证书 W8 未到位 | 计划表已列为甲方截止项；先在临时环境完成全部测试 |
| 分享链接被爬/刷 | 过期 + 密码 + Redis 频控三层，验收用例覆盖"过期不可访问、密码错误拒绝" |
