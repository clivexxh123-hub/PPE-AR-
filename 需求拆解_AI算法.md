# PPE VI 视觉自动化系统 — AI 算法需求拆解

> 与《需求拆解_功能模块.md》配套。本文档只覆盖算法/模型侧，前端交互见功能文档对应 R 编号。
> 技术基线：Flux（渲染模型）+ ComfyUI 工作流 + 公司自研图像识别/控制算法（或双方确认的等效方案）。

---

## AI-1 LOGO 智能抠图（对应 R2.1）

**输入**：PNG/JPG 实物照片，或 SVG/AI/EPS 矢量文件
**输出**：透明底 PNG

拆解：
1. 矢量通道：SVG/AI/EPS 解析 → 直接渲染为透明底 PNG（纯格式转换，不走 AI）
2. 照片通道：AI 识别照片中的 LOGO 区域 → 分割提取 → 去背景 → 边缘精修（去残留底色、抗锯齿）
3. 全自动：上传即处理，无人工步骤；结果进画布预览，可存客户档案

**量化指标（验收）**：

| 指标 | 要求 |
|---|---|
| 矢量文件成功率 | > 95% |
| 实物照片提取成功率 | > 90% |
| 单张处理时间 | < 5 秒 |
| 边缘质量 | 无残留背景色 |

**风险**：复杂背景照片、低分辨率 LOGO 提取失败的兜底交互（提示重传/手动框选）需要定义。

---

## AI-2 撞色检测与自动改色（对应 R2.2）

**输入**：LOGO 透明 PNG + 产品底色
**输出**:改色后 LOGO + 推荐配色

拆解：
1. 提取 LOGO 主色 与 产品印刷面底色
2. 撞色判定（色相/对比度阈值）
3. 按业务配色规则推荐：红/蓝底→印白；白/黄底→印蓝；橙底→印蓝或白
4. 用户可接受/拒绝/自定义颜色/一键还原
5. 改色仅替换色相，保留 LOGO 图形细节（描边、渐变处理策略需定义）

**量化指标**：撞色检测准确率 > 90%；改色后视觉自然；支持一键还原。

---

## AI-3 智能贴图渲染引擎（对应 R4.2、R4.3、M5）

**输入**：产品视角底图 + LOGO/文字图层 + 位置/尺寸参数
**输出**：合成效果图

拆解：
1. **自动定位**：LOGO 默认吸附产品印刷区中心，支持拖拽微调，位置记忆
2. **自动缩放**：按标准版/最大版/最小版尺寸约束，宽度微调
3. **透视变形**：检测产品曲面（安全帽弧面等）→ LOGO 网格变形贴合曲面 → 边缘自然过渡
4. **光影融合**：检测底图光照方向 → LOGO 匹配光照 → 自动加阴影/高光
5. **实时性**：参数调整即时渲染（客户端预览可用低精度，导出走高精度）

**量化指标**：

| 指标 | 要求 |
|---|---|
| 定位精度 | 像素级 |
| 合成出图 | 秒级、无明显延迟 |
| 曲面贴合 | 无生硬感，项目组主观评审通过 |
| 光影 | 方向与产品实拍图一致 |

**风险**：秒级实时 + 高质量透视/光影可能冲突，建议明确"预览低配、导出高配"双档策略并写入验收。

---

## AI-4 AI 生图（模特/场景/全身穿戴）（对应 R6.1、R6.2）

**输入**：产品图（已贴印标）+ 人模选择 + 场景选择 + Prompt
**输出**：人物穿戴产品的场景营销图

拆解：
1. **AI 模特生成**：可选人模库；人物姿态自然站立
2. **AI 场景生成**：行业背景自动匹配（工地、电力、交通…），支持自定义；背景不杂乱
3. **全身穿戴合成**：多产品同时上身（帽+马甲+手套+鞋…），搭配协调、无穿模
4. **印标保真**：贴好的 LOGO/文字在生图后不糊、不变形（核心难点，与 AI-5 联动）
5. **局部特写**：主图全身 + 底部各印刷面局部特写（方案2），放大清晰
6. **多比例导出**：导出时可选多种画幅比例
7. **水印**：自动附"首盾全身安全防护"品牌水印

**量化指标**：

| 指标 | 要求 |
|---|---|
| 单图生成 | < 1 分钟 |
| 全身穿戴渲染 | < 30 秒 |
| 穿戴质量 | 无穿模、佩戴自然、场景真实不失真 |
| 局部标识 | 特写清晰可读 |

---

## AI-5 产品一致性与记忆机制（对应 R6.1，清单 #10/#19）

**问题**：内部自测发现生图后"安全帽前后不一样、产品明显变样"。

拆解：
1. 算法控制 AI 理解，优先级：产品原型不失真不变形 > 人物自然 > 佩戴自然
2. **记忆功能**：按产品沉淀生成参数/LoRA/参考图，越用越精准
3. 产品款式更新后可反馈重训/调参
4. 错误反馈闭环：前端一键反馈生图错误 → 服务商修复

**合同承诺（必须写入验收/维保条款）**：
- AI 生图错误一周内修改完毕
- 交付后 1 年内免费维护
- 公司自研模型升级免费；大版本升级另议付费

---

## AI-6 并发与生成调度（对应 M13，清单 #11）

**目标**：150 人同时使用不崩溃。

拆解：
1. 生成请求进队列，分流调度（第一批即时、后续排队），前端无感 + 等待动效
2. GPU 资源池管理；任务优先级与超时重试
3. 后端实时记录每次生成耗时，落库，支持后续定点扩容优化
4. 崩溃预案：若持续过慢，针对性升级硬件（合同已约定）

**量化指标**：150 并发服务器不崩溃；排队状态前端可见；耗时监控可查询。

---

## 验收指标汇总表（建议直接写入合同附件）

| # | 指标 | 数值 | 来源 |
|---|---|---|---|
| 1 | 矢量抠图成功率 | >95% | 清单#5 |
| 2 | 照片 LOGO 提取成功率 | >90% | 清单#5 |
| 3 | 抠图耗时 | <5s/张 | 清单#5 |
| 4 | 撞色检测准确率 | >90% | 清单#6 |
| 5 | 贴图定位精度 | 像素级 | 清单#11 |
| 6 | 贴图合成 | 秒级出图 | 清单#14 |
| 7 | 单图 AI 生成 | <1 分钟 | 清单#7 |
| 8 | 全身穿戴渲染 | <30 秒 | 清单#17 |
| 9 | 并发能力 | 150 人不崩溃 | 清单#11 |
| 10 | AI 错误修复 | 一周内 | 清单#10 |
| 11 | 免费维护期 | 交付后 1 年 | 清单#10/#19 |
