# AI 算法 — 推荐做法与计划表

> 覆盖《需求拆解_AI算法.md》AI-1 ~ AI-6；配套工程侧见《非AI功能_推荐做法与计划表.md》
> 人力：AI 算法 1.5 人 × 10 周（关键路径）；环境：GPU 服务器 + ComfyUI + Flux

---

## 一、总体策略

1. **能不用 AI 就不用 AI**：抠图矢量通道、撞色检测、贴图透视全部用确定性算法实现，稳定、可测、快。AI 只留给真正需要的：照片抠图、生图。
2. **风险前置**：印标保真（生图后 LOGO 糊/变形）是全项目最大技术风险，W1–2 PoC 必须先验证，不达标立即切"回贴"方案。
3. **每周对指标回归**：W1 建好验收样本集（矢量 LOGO ≥50、实物照片 ≥50、产品 ≥5 款），每周跑一遍 11 项指标，不等 W9。

## 二、逐项推荐做法

### AI-1 LOGO 智能抠图

| 通道 | 推荐方案 | 说明 |
|---|---|---|
| 矢量（SVG/AI/EPS） | **不走 AI**：SVG 用 resvg/CairoSVG 渲染；AI/EPS 用 Ghostscript/Inkscape 转 SVG 再渲染 | 纯格式转换，成功率轻松 >95%，耗时毫秒级 |
| 照片（PNG/JPG） | **RMBG-2.0 或 BiRefNet** 分割去背景 → alpha matting 边缘精修 → 去残留底色（色差阈值清理） | 开源 SOTA 抠图模型，单张 <2s，留足 5s 余量 |
| 兜底 | 置信度低/结果为空 → 前端提示"手动框选"，框选区域再跑一次分割（可接 SAM2 点选） | 需求文档已要求定义此交互，别漏 |

**要点**：抠图结果统一输出 2048px 透明 PNG + 原图存档；处理耗时逐条落库（对指标 #3）。

### AI-2 撞色检测与自动改色 — 不用 AI

1. **主色提取**：LOGO 非透明像素在 **Lab 色彩空间做 K-means**（k=3），取占比最大簇为主色；产品底色从印刷区采样。
2. **撞色判定**：ΔE（CIEDE2000）< 阈值 或 明度对比 < 阈值 → 判撞色。阈值用业务方给的正反例标定（红/蓝底→白、白/黄底→蓝、橙底→蓝/白 这套规则直接做成配置表）。
3. **改色**：HSL 空间只替换色相（H），保留明度/饱和度层次和 alpha → 描边、渐变细节自动保留。多色 LOGO 按簇分别映射。
4. 原色数据不动，改色是渲染层参数 → **一键还原零成本**。

纯算法方案准确率可控、可单测，>90% 指标容易举证。

### AI-3 智能贴图渲染引擎 — 核心是"标定"，不是 AI

**推荐做法：每款产品每个视角做一次性人工标定，运行时用确定性变形。**

1. **印刷区标定工具**（做进管理后台）：运营上传底图后，在图上标 4×4 网格控制点圈定印刷区曲面。一款产品标一次，永久复用。这是把"曲面贴合"从 AI 难题变成工程问题的关键。
2. **透视/曲面变形**：LOGO 按控制点做 **TPS（薄板样条）/ 网格 warp** 贴合曲面；平面产品（马甲、刀旗）退化为单应矩阵，更简单。
3. **光影融合**：从底图印刷区提取亮度层（高斯模糊后的 L 通道），以 multiply 方式叠到 LOGO 上 → 高光阴影自动跟随产品实拍光照，无需检测光照方向。
4. **双档渲染**：预览走前端 WebGL/Canvas 低精度（参数改动即时出图）；导出走后端 sharp/OpenCV 高精度。**此口径 MS2 前与甲方书面确认。**
5. 定位：印刷区中心为默认锚点，拖拽偏移量按客户+产品存库（位置记忆）。

像素级定位、秒级出图两项指标在此方案下是自然结果。

### AI-4 AI 生图（模特/场景/穿戴）

**基线**：Flux.1-dev + ComfyUI 工作流，按场景封装成 API 模板。

1. **人模库**：预生成固定人模（seed + face reference 锁定），用户"选人模"= 选预设，不是每次现生成 → 姿态稳定、速度快。
2. **场景**：行业场景做成 Prompt 模板库（工地/电力/交通…），底模直出；自定义场景走用户 Prompt 拼接 + 负向词兜底"背景不杂乱"。
3. **穿戴合成**：产品图作为参考图输入（Flux Redux / IP-Adapter 路线），多产品分区域 inpaint 上身；ControlNet-Pose 锁人体姿态防穿模。
4. **印标保真 — 推荐"生图后回贴"**：AI 只生成"人穿着该款产品"的图，**LOGO/文字不参与扩散过程**；生图完成后检测产品印刷区位姿，将 AI-3 的贴图结果 warp 回贴。文字和小 LOGO 永不糊。直接生成路线做备选，PoC 阶段两条都跑，W2 定案。
5. **局部特写**：从回贴后高清图按印刷区裁切放大（必要时 Real-ESRGAN 超分），拼到主图底部（方案2 版式固定成模板）。
6. 多比例导出用 outpaint/裁切模板；水印在导出管线最后一步叠加（工程侧做）。

**性能**：单图 <1 分钟 → Flux dev 20~30 步在 A 级卡约 10–20s，留有余量；全身穿戴 <30s 需要控制 inpaint 轮次，PoC 实测定参数。

### AI-5 产品一致性与记忆机制

1. **每款产品训一个 LoRA**（20–40 张多角度图即可，标定时顺手收集），生图时挂载 → "前后不一样、明显变样"问题的主治手段。
2. 生成参数（seed/steps/LoRA 权重/成功 Prompt）按产品落库，好结果一键复用 → "越用越准"的记忆功能本质是**参数沉淀，不是在线学习**，按这个口径实现和验收。
3. 产品款式更新 → 后台触发 LoRA 重训管线（脚本化，30 分钟级）。
4. 错误反馈闭环：前端"生图报错"按钮 → 落库（图 + 参数 + 用户备注）→ 服务商工单，对齐"一周修复"SLA。

### AI-6 并发与生成调度

1. **队列**：Redis + BullMQ；前端提交即返回 task_id，WebSocket/轮询推进度；排队位置可见 + 等待动效。
2. **GPU 池**：ComfyUI 多实例注册为 worker，队列按空闲分发；单卡串行、多卡并行。**150 并发 ≠ 150 张同时生成**，是 150 人在线提交不崩 —— 这个口径要在验收标准里写清楚，否则 GPU 预算失控。
3. 优先级（KA 演示 > 普通）、超时重试（2 次）、死信告警。
4. 每次生成耗时/排队时长落库 → 看板可查，扩容有据（合同已约定硬件升级路径）。

## 三、AI 专线计划表（10 周）

| 周 | 工作内容 | 出口标准 |
|---|---|---|
| **W1** | GPU 环境 + ComfyUI/Flux 部署；建验收样本集；三 PoC 开跑：RMBG 抠图 / TPS 贴图 / 穿戴生图（回贴 vs 直出两条路线） | 环境可用，样本集入库 |
| **W2** | PoC 收口评审：印标保真路线定案（回贴 or 直出）；曲面贴合样例出图 | ⛳ MS1：三链路 Demo 达标，方案定案 |
| **W3** | AI-1 工程化：矢量通道 + 照片通道 + 兜底交互 API；耗时落库 | 抠图 API 可联调，样本集成功率 >90%/95% |
| **W4** | AI-2 撞色检测 + 改色（配置化规则表）；标定工具联调（配合后端）；AI-3 贴图 v1（TPS + 尺寸约束） | 撞色准确率 >90%（样本集）；平面产品贴图可用 |
| **W5** | AI-3 曲面 + 光影融合调优；双档渲染打通；⛳ MS2 核心链路合龙 | 贴图秒级、曲面样例甲方评审通过并签认 |
| **W6** | AI-4 v1：人模库 + 场景模板 + 单产品穿戴 + 回贴管线 | 单图 <1min，印标清晰 |
| **W7** | AI-4 v2：多产品全身穿戴、局部特写、多比例；AI-5：首批 5 款产品 LoRA + 参数沉淀 + 反馈闭环接口 | 穿戴 <30s，无穿模样例过评审 |
| **W8** | AI-6 队列/GPU 池上线；**第一轮 150 并发压测**；全量产品 LoRA 批量训练 | ⛳ MS3：压测报告，瓶颈定位 |
| **W9** | 指标全量自测（11 项逐条）+ 第二轮压测 + UAT 问题修复 | 验收对照表全绿 |
| **W10** | 生产部署调参、终验支持、LoRA 训练管线交接培训 | ⛳ MS4 |

## 四、风险表

| 风险 | 概率 | 对策 |
|---|---|---|
| 直出路线印标必糊 | 高 | 已预设回贴为主路线，W2 定案 |
| 回贴时印刷区位姿检测不准（人体姿态遮挡） | 中 | 生图时用 ControlNet 锁定产品朝向；检测失败自动重生成 |
| 全身多产品穿戴 <30s 不达标 | 中 | 减少 inpaint 轮次/降步数 + 超分补偿；必要时与甲方重议该项口径 |
| GPU 预算不足支撑压测目标 | 中 | W8 首轮压测即出扩容清单，走合同硬件升级条款 |
| "视觉自然/无生硬感"等主观项扯皮 | 高 | 每里程碑样例图双方签认为基准，之后只比对基准 |
